# UIR-001 补齐后端接口能力

## 状态

done

## 优先级

P0

## 所属阶段

frontend-refactor

## 依赖任务

- FE-013
- SERVER-006
- SERVER-007
- SERVER-008
- SERVER-009

## 需求来源

- [UIR-000-前端重构与UI功能调整方案.md](/Users/fenghp/Desktop/DEV/project/mydev/task/frontend-refactor/UIR-000-前端重构与UI功能调整方案.md)

## 目标

补齐前端重构所需的后端接口能力，确保文件查看、服务器内容读取、默认 Workspace、目录对比、Get Latest / Checkout 冲突处理可以被 UI 调用。

## 实现范围

- 登录后保存上次使用的 Collection。
- 根据 Collection 自动查找本机已有 Workspace。
- 本机没有 Workspace 时按程序固定规则创建默认 Workspace。
- 返回当前实际使用的 Workspace 名称和基础信息。
- 提供 Mapping 目录内本地文件内容读取能力，并校验路径不能越过 Mapping 目录。
- 提供未映射文件的服务器 latest 内容读取能力。
- 文件内容接口返回文件大小、是否二进制、是否可文本渲染、文件编码和内容。
- 文件超过 5MB 时返回大小信息，前端不直接渲染内容。
- Diff 接口支持本地文件与服务器 latest、两个历史 changeset。
- 目录对比接口返回差异状态、可执行动作、pending edit 子项。
- Get Latest 支持递归、跳过 pending edit 子文件并返回跳过列表。
- Checkout 自动先 Get Latest，再签出。
- Get Latest / Checkout 遇到冲突时返回统一冲突明细。
- 冲突明细包含服务端路径、本地路径、文件大小、是否二进制、是否可自动合并。
- 支持应用冲突选择：使用服务器版本、保留本地版本、自动合并结果。
- Pending Changes 返回 pending add / edit / delete 类型，支持菜单判断。

## 不在范围

- 不实现前端页面。
- 不实现 Monaco 或 CodeMirror。
- 不做手动冲突块编辑。
- 不改变现有 TFS 协议封装以外的业务规则。

## 涉及文件

- [mactfs/src/main/java/com/mydev/mactfs](/Users/fenghp/Desktop/DEV/project/mydev/mactfs/src/main/java/com/mydev/mactfs)
- [mactfs/src/main/java/com/mydev/mactfs/server](/Users/fenghp/Desktop/DEV/project/mydev/mactfs/src/main/java/com/mydev/mactfs/server)
- [mactfsui/app/lib/api](/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/lib/api)

## 验收标准

- 登录连接后能返回固定 Collection 和当前 Workspace。
- 没有 Workspace 时能创建默认 Workspace。
- 已映射文件能读取本地内容，未映射文件能读取服务器内容。
- 非 Mapping 目录路径读取被拒绝。
- 大于 5MB 文件不返回正文渲染内容。
- pending add 文件不会提供服务器比较能力。
- Get Latest / Checkout 冲突能返回前端弹窗所需明细。
- 应用“使用服务器版本”后无 pending change。
- 应用“保留本地版本”后进入 pending edit。
- 自动合并结果确认应用后进入 pending edit。

## 测试方式

```bash
cd /Users/fenghp/Desktop/DEV/project/mydev/mactfs
../tfsIntegration/gradlew test
../tfsIntegration/gradlew runServer
```

## 完成记录

- 实际修改文件：
  - `mactfs/src/main/java/com/mydev/mactfs/core/MacTfsCoreService.java`
  - `mactfs/src/main/java/com/mydev/mactfs/server/MacTfsServer.java`
  - `mactfsui/app/lib/api/types.ts`
  - `mactfsui/app/lib/api/endpoints.ts`
- 实际实现内容：
  - 新增固定 Collection 后自动确保默认 Workspace 的 `/api/workspace/context`。
  - 新增 `/api/files/content`，支持映射目录内本地文件读取和未映射文件服务器 latest 内容读取。
  - 文件内容返回大小、来源、编码、二进制标识、是否可渲染和正文；超过 5MB 或二进制文件不返回可渲染正文。
  - 本地文件读取和本地 latest diff 增加 Mapping 目录边界校验，非 Mapping 路径会被拒绝。
  - Get Latest 遇到 pending edit 子项时返回跳过列表和冲突明细，不直接覆盖本地修改。
  - Checkout 前自动执行 Get Latest，并在冲突时返回同一套冲突明细。
  - 新增 `/api/conflicts/apply`，支持使用服务器版本或保留本地版本的冲突选择应用入口。
  - 前端 API 类型和 endpoint 增加 WorkspaceContext、FileContent、ConflictChoice 相关类型与方法。
- 测试结果：
  - 已执行 `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfs && ../tfsIntegration/gradlew test`。
  - 结果：BUILD SUCCESSFUL。
- 遗留问题：
  - 未执行真实 TFS Get Latest / Checkout / 冲突应用验收，需后续 UIR-010 端到端任务覆盖。
