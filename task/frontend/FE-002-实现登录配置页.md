# FE-002 实现登录配置页

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-001
- SERVER-003
- SERVER-004

## 需求来源

- PRD 七、7.1 首次连接

## 目标

实现输入 TFS 地址、账号、密码并连接服务器的 UI。

## 实现范围

- serverUri 输入
- authType 选择
- domain 输入
- username 输入
- password 输入
- 连接按钮
- 连接中状态
- 错误提示
- 成功后保存默认配置

## 不在范围

- 不做多 Profile
- 不做 Keychain
- 不做复杂账号管理

## 涉及文件

- [mactfsui/app](/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app)

## 验收标准

- 可填写配置并调用连接 API
- 连接成功进入主界面
- 连接失败显示错误
- 再次打开能回填配置

## 测试方式

```bash
cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui
pnpm typecheck
```

## 完成记录

- 完成日期：2026-06-08
- 实际修改文件：
  - `mactfsui/app/lib/api/types.ts`
  - `mactfsui/app/lib/api/endpoints.ts`
  - `mactfsui/app/routes/home.tsx`
  - `task/README.md`
  - `task/frontend/FE-002-实现登录配置页.md`
- 实际实现内容：
  - 新增 `AppConfig`、`ConfigData`、`ConnectData` 等前端 API 类型。
  - 新增 `getConfig`、`connectSession` API endpoint 封装，复用 FE-001 的统一 `apiRequest` 自动 Bearer token 能力。
  - 首页增加连接配置表单，支持 `serverUri`、`authType`、`domain`、`username`、`password`。
  - 页面初始化时在本地服务可用后读取 `/api/config`，将默认配置回填到表单。
  - 点击连接后调用 `/api/session/connect`，展示连接中状态；失败展示后端错误，成功进入已连接工作台状态。
  - 保留右侧本地服务状态面板，服务未启动或 preload 缺失时继续显示明确提示。
- 已执行测试：
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && pnpm typecheck`：通过。
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && node -c electron/main.cjs && node -c electron/preload.cjs`：通过。
  - `git diff --check`：通过。
  - 内置浏览器打开 `http://127.0.0.1:5173/`：通过，连接表单与服务状态面板可见，非 Electron 环境显示 preload 缺失提示。
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && pnpm electron:dev`：通过，Electron 启动后本地 API health 与 config 接口可访问。
  - 使用无效 TFS 地址请求 `/api/session/connect`：通过，后端返回 `success=false` 和错误信息，配置未被保存。
- 未执行测试及原因：
  - 未执行真实 TFS 账号的连接成功验证；当前环境没有可用于验收的真实 TFS 凭据，且任务规则要求谨慎处理真实 TFS 操作。
- 验收标准确认：
  - 可填写配置并调用连接 API：满足，表单字段完整，连接提交走 `connectSession`。
  - 连接成功进入主界面：满足，`connectSession` 返回成功后进入已连接工作台状态；真实 TFS 成功连接未在当前环境执行。
  - 连接失败显示错误：满足，无效连接返回的错误会写入页面错误提示。
  - 再次打开能回填配置：满足，页面初始化读取 `/api/config` 并回填表单。
- 遗留问题：
  - 真实 TFS 成功连接需要在具备有效 TFS 地址和账号后做端到端验收。
