# FE-029 工作台外壳与顶栏改版

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-026
- FE-027

## 需求来源

- [docs/mactfs-ui-restyle-requirements.md](/Users/fenghp/Desktop/DEV/project/mydev/docs/mactfs-ui-restyle-requirements.md) U4

## 目标

把三栏工作台外壳与顶栏升级为 macOS 工具栏 / Finder 侧栏风格，并补齐布局动效。

## 实现范围

- 顶栏按 macOS 工具栏风格重排：为红绿灯预留左侧空间，上下文信息居左、动作按钮图标化居右
- 左侧目录树面板改 Finder 式侧栏：毛玻璃背景（接 FE-027 vibrancy）、圆角选中条、分组标题样式
- 左右面板折叠/展开增加宽度过渡动画，底部操作台增加滑入滑出动画
- 统一三栏分隔线、内边距与留白

## 不在范围

- 目录树节点与文件列表行内样式（FE-030 做）
- 弹窗样式（FE-031 做）
- 不改动作编排与状态模型

## 涉及文件

- `mactfsui/app/components/app/workspace-shell.tsx`
- `mactfsui/app/components/app/top-bar.tsx`

## 验收标准

- 顶栏与红绿灯融合自然，动作按钮可用且有 tooltip
- 侧栏呈毛玻璃 Finder 风格
- 折叠/展开、底部操作台均有流畅动画且不卡顿
- 同步导航、刷新等既有行为无回归

## 测试方式

```bash
cd mactfsui && pnpm typecheck && pnpm electron:dev
# 手工验证：折叠/展开左右面板与底部操作台，检查动画与功能
```

## 完成记录

- 实际修改文件：
  - `mactfsui/app/components/app/workspace-shell.tsx`
  - `mactfsui/app/components/app/top-bar.tsx`
  - `mactfsui/app/components/explorer/source-tree-panel.tsx`（面板标题样式）
  - `mactfsui/app/components/inspector/changes-panel.tsx`（改为不透明底 + 标题样式）
  - `mactfsui/app/components/logs/console-panel.tsx`（高度交由外层控制 + 标题样式）
- 实际实现内容：
  - Finder 式布局：根容器不铺底色，vibrancy 下顶栏整条与左侧目录树呈窗口级毛玻璃；中间列表 / 右侧挂起 / 底部日志保持不透明内容底
  - 顶栏透明融入材质，红绿灯左侧留白，动作按钮图标化（工作区→FolderCog、重新连接→Unplug）配 tooltip
  - 左右面板折叠改保持挂载 + 宽度过渡（250ms ease-out-quart），底部操作台高度过渡滑入滑出
  - 三个面板标题统一小字号大写风格
  - 非 macOS / 非 Electron 环境 body 实色回退，布局不受影响
- 测试结果：
  - `pnpm typecheck` 通过、`pnpm build` 通过
  - 毛玻璃整体效果在 FE-034 回归截图确认
- 遗留问题：
  - 顶部通知条仍为旧样式，FE-032 用 toast 替换
