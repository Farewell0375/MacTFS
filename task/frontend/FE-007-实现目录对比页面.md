# FE-007 实现目录对比页面

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-005
- SERVER-008

## 需求来源

- PRD 六、6.4 目录对比

## 目标

实现已映射目录的目录对比结果展示和差异文件勾选。

## 实现范围

- 对已映射目录显示“目录对比”
- 请求期间展示 TFS 网络等待提示
- 展示差异文件列表
- 默认隐藏 upToDate
- 支持勾选差异文件
- 根据状态显示可用操作

## 不在范围

- 不做自动修复全部
- 不做实时 watcher
- 不做目录级完整 diff

## 涉及文件

- [mactfsui/app](/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app)

## 验收标准

- 可展示 localModified、localOnly、notDownloaded、pending 等状态
- 可勾选指定差异文件
- 未映射目录不能执行对比

## 测试方式

```bash
cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui
pnpm typecheck
```

## 完成记录

- 实际修改文件：
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/lib/api/types.ts`
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/lib/api/endpoints.ts`
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/components/explorer/folder-items-panel.tsx`
  - `/Users/fenghp/Desktop/DEV/project/mydev/task/README.md`
  - `/Users/fenghp/Desktop/DEV/project/mydev/task/frontend/FE-007-实现目录对比页面.md`
- 实际实现内容：
  - 新增目录对比请求、响应和差异状态类型。
  - 新增 `compareFolder` 统一 API 封装，对接 `POST /api/compare/folder` 的 `diffs` 返回结构。
  - 在中间目录文件面板增加“目录对比”区域，已映射目录可执行 Compare，未映射目录仅展示不可对比状态。
  - 目录对比请求期间展示“正在执行目录对比，TFS 网络请求可能需要一段时间。”。
  - 对比结果默认隐藏 `upToDate`，支持切换显示状态。
  - 差异列表支持逐项勾选和当前筛选结果批量勾选。
  - 按状态展示可用操作入口：`checkout`、`diff`、`add`、`delete local`、`delete`、`restore from server`、`get latest`、`checkin`、`undo`、`手工处理`。
- 已执行测试：
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && pnpm typecheck`：通过。
  - `git diff --check`：通过。
  - Browser 打开 `http://127.0.0.1:5173/`：页面工作台结构可渲染，连接页、Source Workspace、Inspector、Console 可见。
- 未执行测试及原因：
  - 未执行真实 TFS 目录对比请求；当前环境没有真实 TFS 凭据、已连接会话和真实 Mapping，且该操作依赖外部 TFS 网络状态。
  - Browser 截图调用超时，但 DOM 快照已确认本地页面基础结构渲染正常。
- 是否满足验收标准：
  - 可展示 `localModified`、`localOnly`、`notDownloaded`、`pendingEdit`、`pendingAdd`、`pendingDelete` 等状态文案和操作入口。
  - 可勾选指定差异文件，并可批量勾选当前筛选结果。
  - 未映射目录不显示 Compare 执行按钮，并展示不能执行对比的状态。
- 遗留问题：
  - 当前任务只展示可用操作入口，不执行真实文件操作；具体操作交互属于后续 FE-009。
  - 真实 TFS 成功路径需要在具备测试 Collection、Workspace 和 Mapping 后做端到端验收。
