# FE-005 实现中间目录文件列表

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-004
- SERVER-005
- SERVER-006

## 需求来源

- PRD 六、6.3 中间文件列表

## 目标

实现点击左侧目录后，中间展示当前目录下一级文件和文件夹。

## 实现范围

- 展示名称、类型、服务端路径、本地路径、是否已映射、状态
- 已映射但本地不存在显示 `notDownloaded / 未下载`
- 支持文件 / 目录选中
- 根据映射状态展示基础操作入口

## 不在范围

- 不做目录对比详情
- 不做 diff 页面
- 不做 pending changes 面板

## 涉及文件

- [mactfsui/app](/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app)

## 验收标准

- 目录点击后中间列表更新
- 未映射目录不展示本地操作
- 已映射未下载项状态清晰

## 测试方式

```bash
cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui
pnpm typecheck
```

## 完成记录

- 完成日期：2026-06-08
- 实际修改文件：
  - `mactfsui/app/lib/api/types.ts`
  - `mactfsui/app/lib/api/endpoints.ts`
  - `mactfsui/app/components/explorer/folder-items-panel.tsx`
  - `mactfsui/app/routes/home.tsx`
  - `task/README.md`
  - `task/frontend/FE-005-实现中间目录文件列表.md`
- 实际实现内容：
  - 新增 Mapping 与目录文件列表相关前端类型。
  - 新增 `listMappings`、`listServerFolderItems` API endpoint 封装。
  - 新增 `FolderItemsPanel`，根据当前 Collection 和服务端路径加载下一级文件 / 文件夹。
  - 中间列表展示名称、类型、服务端路径、本地路径、映射状态、状态。
  - 根据 Mapping 计算本地路径；已映射项展示 `notDownloaded / 未下载` 状态，未映射项展示 `未映射`。
  - 支持文件 / 目录行选中，并在底部展示已选中路径。
  - 基础操作入口按映射状态展示：未映射项只保留 History，已映射项显示 Get Latest / Checkout 入口。
- 已执行测试：
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && pnpm typecheck`：通过。
  - `git diff --check`：通过。
  - `find /Users/fenghp/Desktop/DEV/project/mydev/task -type f -name "*.md" | wc -l`：通过，结果为 `50`。
  - 内置浏览器打开 `http://127.0.0.1:5173/`：通过，未连接状态下主布局和中间区域稳定显示。
- 未执行测试及原因：
  - 未执行真实 TFS 目录点击后加载文件列表；当前环境没有可用于验收的真实 TFS 连接配置。
  - 未验证真实本地文件是否存在；当前后端目录接口未返回本地存在状态，前端只能根据 Mapping 计算本地路径并展示 `notDownloaded / 未下载` 的基础状态。
- 验收标准确认：
  - 目录点击后中间列表更新：代码路径满足，左侧选中路径会传给 `FolderItemsPanel` 并触发 `/api/server-folder/items`。
  - 未映射目录不展示本地操作：满足，未映射项不显示 Get Latest / Checkout 本地操作入口。
  - 已映射未下载项状态清晰：满足，已映射项展示 `notDownloaded / 未下载` 状态；真实本地存在性需后续后端状态字段或目录对比补强。
- 遗留问题：
  - 真实 TFS 目录文件列表、真实本地存在状态需要后续端到端环境补验。
