# FE-011 实现历史记录界面

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-005
- SERVER-009

## 需求来源

- PRD 六、6.10 History

## 目标

实现文件历史、目录历史和 changeset 文件列表界面。

## 实现范围

- 文件历史最近 100 条
- 目录历史最近 100 条
- 展示 changeset、作者、时间、comment
- 点击目录历史 changeset 展示影响文件列表
- 从历史文件列表进入 diff

## 不在范围

- 不做筛选
- 不做 Work Item
- 不做历史树

## 涉及文件

- [mactfsui/app](/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app)

## 验收标准

- 文件可查看历史
- 目录可查看历史
- 目录历史 changeset 可展开文件列表
- 可选择历史版本进入 diff

## 测试方式

```bash
cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui
pnpm typecheck
```

## 完成记录

- 实际修改文件：
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/lib/api/types.ts`
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/lib/api/endpoints.ts`
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/components/explorer/folder-items-panel.tsx`
  - `/Users/fenghp/Desktop/DEV/project/mydev/mactfsui/app/components/explorer/history-panel.tsx`
  - `/Users/fenghp/Desktop/DEV/project/mydev/task/README.md`
  - `/Users/fenghp/Desktop/DEV/project/mydev/task/frontend/FE-011-实现历史记录界面.md`
- 实际实现内容：
  - 新增 `TfsHistoryEntry`、`HistoryData`、`ChangesetFilesData` 类型。
  - 新增 `queryHistory` 和 `queryChangesetFiles` API 封装。
  - 新增 `HistoryPanel`，展示文件或目录最近历史记录。
  - 历史列表展示 changeset、变更类型、作者、时间、comment。
  - 当前目录和选中文件 / 目录均可打开 History。
  - 目录历史可点击 changeset 查看影响文件列表。
  - 文件历史支持选择两个历史版本并进入 Diff 入口。
  - changeset 文件列表支持单文件 Diff 入口提示；真实 Diff 渲染由后续 FE-012 接入。
- 已执行测试：
  - `cd /Users/fenghp/Desktop/DEV/project/mydev/mactfsui && pnpm typecheck`：通过。
  - `git diff --check`：通过。
  - Browser 打开 `http://127.0.0.1:5173/`：工作台、Inspector、Source Workspace 基础结构可渲染；当前非 Electron 环境显示未连接状态。
- 未执行测试及原因：
  - 未执行真实历史查询成功路径；当前环境没有真实 TFS 凭据、连接会话和可查询的 TFS 路径。
  - 未执行真实 Diff 渲染；完整 Diff 界面属于后续 FE-012。
- 是否满足验收标准：
  - 文件可打开历史入口。
  - 目录可打开历史入口。
  - 目录历史 changeset 可查询并展示影响文件列表。
  - 可选择历史版本或 changeset 文件进入 Diff 入口。
- 遗留问题：
  - 历史 Diff 当前只记录入口选择，不渲染实际 diff 内容，待 FE-012 完成。
  - 真实 TFS 历史成功路径需要在后续端到端验收中验证。
