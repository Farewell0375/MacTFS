# FE-028 重设计登录页

## 状态

done

## 优先级

P0

## 所属阶段

frontend

## 依赖任务

- FE-026

## 需求来源

- [docs/mactfs-ui-restyle-requirements.md](/Users/fenghp/Desktop/DEV/project/mydev/docs/mactfs-ui-restyle-requirements.md) U3

## 目标

把登录页从单张表单卡片升级为有品牌感、有动效、元素丰富的现代登录页。

## 实现范围

- 左右分栏布局：左侧品牌区（logo、产品名、标语、版本号、氛围背景动效），右侧连接表单卡片
- 表单 → Collection 选择两阶段切换增加滑动 + 淡入淡出过渡
- 新增：密码可见性切换按钮、最近连接记录（localStorage 多条、一键填充）、本地服务健康状态指示灯
- 连接中按钮内 spinner、阶段文案过渡；连接失败表单抖动 + 错误提示条
- Collection 列表改卡片式：悬停浮起、选中高亮过渡
- 保持现有 `api.connect / listCollections / ensureWorkspace` 调用链路不变

## 不在范围

- 不改连接协议与认证方式
- 不改 `app/lib/api` 接口契约（最近连接仅存前端本地）

## 涉及文件

- `mactfsui/app/components/app/connect-view.tsx`
- 新增 `mactfsui/app/lib/recent-servers.ts`（或等价的本地存储工具）

## 验收标准

- 登录页具备品牌区与氛围背景，观感现代精致
- 两阶段切换有流畅过渡动效
- 最近连接可保存、可一键填充；密码可见性切换可用
- 连接成功进入工作台的链路与改版前完全一致

## 测试方式

```bash
cd mactfsui && pnpm typecheck && pnpm electron:dev
# 手工验证：连接真实 TFS → 选择 Collection → 进入工作台
```

## 完成记录

- 实际修改文件：
  - `mactfsui/app/components/app/connect-view.tsx`（整体重写）
  - 新增 `mactfsui/app/lib/recent-servers.ts`
  - `mactfsui/app/app.css`（shake 抖动动画令牌）
- 实际实现内容：
  - 左右分栏：左侧 `bg-sidebar` 半透明品牌区（vibrancy 下呈毛玻璃），居中大 logo（drop-shadow 跟随透明轮廓）+「MacTFS」+ 一句话描述 + 底部服务健康指示灯（吸收现代风分支用户反馈：品牌区只放大 logo + 一句话）
  - 表单 → Collection 两阶段滑动淡入过渡；密码可见性切换；最近连接（localStorage 5 条去重置顶、一键填充、不存密码）
  - 连接中按钮 spinner + 阶段文案；失败错误条 + 表单抖动
  - Collection 卡片式选择：选中 = 主色细边框 + 浅主色底 + 蓝色对勾（zoom 入场），未选中 hover 仅底色微变（直接采用对比分支已确认的选中样式）
  - 登录页顶部 `app-drag` 拖拽条（补 FE-027 遗留）
  - `api.connect / listCollections / ensureWorkspace` 链路不变
- 测试结果：
  - `pnpm typecheck` 通过、`pnpm build` 通过，shake 动画已编译
  - 真实连接流程随 FE-034 回归验证
- 遗留问题：
  - 无
